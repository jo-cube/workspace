# Runtime state
workspace/*
!workspace/.gitkeep

# Optional runtime config
runtime-config/*
!runtime-config/.gitkeep

# Secrets (never commit)
secrets/

# Real CA certificates (use CI/CD secrets)
config/ca-certificates/*.crt

# Docker
.env

# OS
.DS_Store
